export interface GlobalStateData{
    country ?: string ,
    state ?: string ,
    lat ?: number, 
    long ?: number,
    confirmed ?: number , 
    deaths ?: number, 
    recovered ?:number , 
    active ?: number 
}